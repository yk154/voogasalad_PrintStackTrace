package groovy.api;

/**
 * Represents the set of possible ports from a GroovyBlock
 */
public enum Ports {
    FLOW_OUT,

    IF_PREDICATE,
    IF_BODY,

    FOREACH_LIST,
    FOREACH_BODY,

    ASSIGN_LHS,
    ASSIGN_RHS,

    A,
    B,
    C,
    D,
    E
}
